{
  "name": "vault-env",
  "version": "0.1.0",
  "description": "Read and set environmental variables for processes from Vault",
  "main": "./lib/VaultEnv.js",
  "scripts": {
    "test": "mocha test/**/*.test.js",
    "test-ci": "set -x && npm update autochecker -q --no-progress && export TMPDIR=$(mktemp -d) && autochecker 5-wheezy 6-wheezy 8-wheezy; e=$?; rm -Rf ${TMPDIR}; exit $e"
  },
  "repository": {
    "type": "git",
    "url": "ssh://git@git.namecheap.net/namp/ecs_deploy.git"
  },
  "author": "Vladimir Doba <vladimir.doba@namecheap.com>",
  "license": "UNLICENSED",
  "bin": {
    "vault-env": "./bin/cli.js"
  },
  "dependencies": {
    "autochecker": "^0.9.2",
    "commander": "^2.10.0",
    "lodash": "^4.17.4",
    "mockery": "^2.1.0",
    "node-vault-client": "^0.2.0",
    "winston": "^2.3.1"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "memory-streams": "^0.1.2",
    "mocha": "^3.2.0"
  }
}
